export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--ivory)] px-6">
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-[var(--forest-800)]">
        Loading AAN Legacy Foundation
      </p>
    </main>
  );
}
